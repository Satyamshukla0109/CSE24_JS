var age = 16;
if( age >= 18 ) {
console.log("<b>You are eligible to cast the vote.</b>");
}
else if(age === 17){
console.log("<b>You are eligible to cast the vote but you have to wait for 1 year.</b>");
}
else{
console.log("<b>You are not eligible to cast the vote.</b>");
}